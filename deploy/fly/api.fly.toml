app = "trythissoftware-api"

[build]
  dockerfile = "Dockerfile"

[env]
  DATABASE_URL = "postgres://..."
  BASE_DOMAIN = "trythissoftware.com"

[http_service]
  internal_port = 8080
  force_https = true
